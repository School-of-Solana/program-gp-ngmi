/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { RAFFLE_VAULT_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLAIM_RAFFLE_DISCRIMINATOR = new Uint8Array([
  193, 113, 8, 101, 7, 208, 64, 116,
]);

export function getClaimRaffleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_RAFFLE_DISCRIMINATOR
  );
}

export type ClaimRaffleInstruction<
  TProgram extends string = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountWinner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountWinner extends string
        ? WritableSignerAccount<TAccountWinner> &
            AccountSignerMeta<TAccountWinner>
        : TAccountWinner,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimRaffleInstructionData = { discriminator: ReadonlyUint8Array };

export type ClaimRaffleInstructionDataArgs = {};

export function getClaimRaffleInstructionDataEncoder(): FixedSizeEncoder<ClaimRaffleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLAIM_RAFFLE_DISCRIMINATOR })
  );
}

export function getClaimRaffleInstructionDataDecoder(): FixedSizeDecoder<ClaimRaffleInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimRaffleInstructionDataCodec(): FixedSizeCodec<
  ClaimRaffleInstructionDataArgs,
  ClaimRaffleInstructionData
> {
  return combineCodec(
    getClaimRaffleInstructionDataEncoder(),
    getClaimRaffleInstructionDataDecoder()
  );
}

export type ClaimRaffleInput<
  TAccountVault extends string = string,
  TAccountWinner extends string = string,
> = {
  vault: Address<TAccountVault>;
  winner: TransactionSigner<TAccountWinner>;
};

export function getClaimRaffleInstruction<
  TAccountVault extends string,
  TAccountWinner extends string,
  TProgramAddress extends Address = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
>(
  input: ClaimRaffleInput<TAccountVault, TAccountWinner>,
  config?: { programAddress?: TProgramAddress }
): ClaimRaffleInstruction<TProgramAddress, TAccountVault, TAccountWinner> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    winner: { value: input.winner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.vault), getAccountMeta(accounts.winner)],
    data: getClaimRaffleInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimRaffleInstruction<TProgramAddress, TAccountVault, TAccountWinner>);
}

export type ParsedClaimRaffleInstruction<
  TProgram extends string = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    winner: TAccountMetas[1];
  };
  data: ClaimRaffleInstructionData;
};

export function parseClaimRaffleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClaimRaffleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vault: getNextAccount(), winner: getNextAccount() },
    data: getClaimRaffleInstructionDataDecoder().decode(instruction.data),
  };
}
