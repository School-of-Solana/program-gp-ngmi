/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { RAFFLE_VAULT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const PAYOUT_RAFFLE_DISCRIMINATOR = new Uint8Array([
  167, 137, 113, 225, 81, 253, 87, 9,
]);

export function getPayoutRaffleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PAYOUT_RAFFLE_DISCRIMINATOR
  );
}

export type PayoutRaffleInstruction<
  TProgram extends string = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type PayoutRaffleInstructionData = { discriminator: ReadonlyUint8Array };

export type PayoutRaffleInstructionDataArgs = {};

export function getPayoutRaffleInstructionDataEncoder(): FixedSizeEncoder<PayoutRaffleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PAYOUT_RAFFLE_DISCRIMINATOR })
  );
}

export function getPayoutRaffleInstructionDataDecoder(): FixedSizeDecoder<PayoutRaffleInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPayoutRaffleInstructionDataCodec(): FixedSizeCodec<
  PayoutRaffleInstructionDataArgs,
  PayoutRaffleInstructionData
> {
  return combineCodec(
    getPayoutRaffleInstructionDataEncoder(),
    getPayoutRaffleInstructionDataDecoder()
  );
}

export type PayoutRaffleAsyncInput<
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  vault?: Address<TAccountVault>;
};

export async function getPayoutRaffleInstructionAsync<
  TAccountAuthority extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
>(
  input: PayoutRaffleAsyncInput<TAccountAuthority, TAccountVault>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  PayoutRaffleInstruction<TProgramAddress, TAccountAuthority, TAccountVault>
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.vault),
    ],
    data: getPayoutRaffleInstructionDataEncoder().encode({}),
    programAddress,
  } as PayoutRaffleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVault
  >);
}

export type PayoutRaffleInput<
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  vault: Address<TAccountVault>;
};

export function getPayoutRaffleInstruction<
  TAccountAuthority extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
>(
  input: PayoutRaffleInput<TAccountAuthority, TAccountVault>,
  config?: { programAddress?: TProgramAddress }
): PayoutRaffleInstruction<TProgramAddress, TAccountAuthority, TAccountVault> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.vault),
    ],
    data: getPayoutRaffleInstructionDataEncoder().encode({}),
    programAddress,
  } as PayoutRaffleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVault
  >);
}

export type ParsedPayoutRaffleInstruction<
  TProgram extends string = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    vault: TAccountMetas[1];
  };
  data: PayoutRaffleInstructionData;
};

export function parsePayoutRaffleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedPayoutRaffleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), vault: getNextAccount() },
    data: getPayoutRaffleInstructionDataDecoder().decode(instruction.data),
  };
}
