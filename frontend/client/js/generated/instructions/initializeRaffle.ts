/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { RAFFLE_VAULT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const INITIALIZE_RAFFLE_DISCRIMINATOR = new Uint8Array([
  110, 142, 92, 16, 15, 58, 89, 229,
]);

export function getInitializeRaffleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_RAFFLE_DISCRIMINATOR
  );
}

export type InitializeRaffleInstruction<
  TProgram extends string = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeRaffleInstructionData = {
  discriminator: ReadonlyUint8Array;
  ticketPrice: bigint;
  maxTickets: number;
  durationSeconds: Option<bigint>;
};

export type InitializeRaffleInstructionDataArgs = {
  ticketPrice: number | bigint;
  maxTickets: number;
  durationSeconds: OptionOrNullable<number | bigint>;
};

export function getInitializeRaffleInstructionDataEncoder(): Encoder<InitializeRaffleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['ticketPrice', getU64Encoder()],
      ['maxTickets', getU32Encoder()],
      ['durationSeconds', getOptionEncoder(getI64Encoder())],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_RAFFLE_DISCRIMINATOR })
  );
}

export function getInitializeRaffleInstructionDataDecoder(): Decoder<InitializeRaffleInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['ticketPrice', getU64Decoder()],
    ['maxTickets', getU32Decoder()],
    ['durationSeconds', getOptionDecoder(getI64Decoder())],
  ]);
}

export function getInitializeRaffleInstructionDataCodec(): Codec<
  InitializeRaffleInstructionDataArgs,
  InitializeRaffleInstructionData
> {
  return combineCodec(
    getInitializeRaffleInstructionDataEncoder(),
    getInitializeRaffleInstructionDataDecoder()
  );
}

export type InitializeRaffleAsyncInput<
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  vault?: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  ticketPrice: InitializeRaffleInstructionDataArgs['ticketPrice'];
  maxTickets: InitializeRaffleInstructionDataArgs['maxTickets'];
  durationSeconds: InitializeRaffleInstructionDataArgs['durationSeconds'];
};

export async function getInitializeRaffleInstructionAsync<
  TAccountAuthority extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
>(
  input: InitializeRaffleAsyncInput<
    TAccountAuthority,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitializeRaffleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeRaffleInstructionDataEncoder().encode(
      args as InitializeRaffleInstructionDataArgs
    ),
    programAddress,
  } as InitializeRaffleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type InitializeRaffleInput<
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  ticketPrice: InitializeRaffleInstructionDataArgs['ticketPrice'];
  maxTickets: InitializeRaffleInstructionDataArgs['maxTickets'];
  durationSeconds: InitializeRaffleInstructionDataArgs['durationSeconds'];
};

export function getInitializeRaffleInstruction<
  TAccountAuthority extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
>(
  input: InitializeRaffleInput<
    TAccountAuthority,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeRaffleInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeRaffleInstructionDataEncoder().encode(
      args as InitializeRaffleInstructionDataArgs
    ),
    programAddress,
  } as InitializeRaffleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeRaffleInstruction<
  TProgram extends string = typeof RAFFLE_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    vault: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeRaffleInstructionData;
};

export function parseInitializeRaffleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitializeRaffleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeRaffleInstructionDataDecoder().decode(instruction.data),
  };
}
