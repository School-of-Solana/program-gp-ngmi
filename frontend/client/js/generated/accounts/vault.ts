/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from 'gill';
import {
  getTicketEntryDecoder,
  getTicketEntryEncoder,
  getVaultStatusDecoder,
  getVaultStatusEncoder,
  type TicketEntry,
  type TicketEntryArgs,
  type VaultStatus,
  type VaultStatusArgs,
} from '../types';

export const VAULT_DISCRIMINATOR = new Uint8Array([
  211, 8, 232, 43, 2, 152, 117, 119,
]);

export function getVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VAULT_DISCRIMINATOR);
}

export type Vault = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  pot: bigint;
  ticketPrice: bigint;
  maxTickets: number;
  ticketCount: number;
  endTime: bigint;
  bump: number;
  winner: Address;
  tickets: Array<TicketEntry>;
  pendingWinner: Option<Address>;
  pendingPrize: bigint;
  paidOut: boolean;
  status: VaultStatus;
};

export type VaultArgs = {
  authority: Address;
  pot: number | bigint;
  ticketPrice: number | bigint;
  maxTickets: number;
  ticketCount: number;
  endTime: number | bigint;
  bump: number;
  winner: Address;
  tickets: Array<TicketEntryArgs>;
  pendingWinner: OptionOrNullable<Address>;
  pendingPrize: number | bigint;
  paidOut: boolean;
  status: VaultStatusArgs;
};

/** Gets the encoder for {@link VaultArgs} account data. */
export function getVaultEncoder(): Encoder<VaultArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['authority', getAddressEncoder()],
      ['pot', getU64Encoder()],
      ['ticketPrice', getU64Encoder()],
      ['maxTickets', getU32Encoder()],
      ['ticketCount', getU32Encoder()],
      ['endTime', getI64Encoder()],
      ['bump', getU8Encoder()],
      ['winner', getAddressEncoder()],
      ['tickets', getArrayEncoder(getTicketEntryEncoder())],
      ['pendingWinner', getOptionEncoder(getAddressEncoder())],
      ['pendingPrize', getU64Encoder()],
      ['paidOut', getBooleanEncoder()],
      ['status', getVaultStatusEncoder()],
    ]),
    (value) => ({ ...value, discriminator: VAULT_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link Vault} account data. */
export function getVaultDecoder(): Decoder<Vault> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['authority', getAddressDecoder()],
    ['pot', getU64Decoder()],
    ['ticketPrice', getU64Decoder()],
    ['maxTickets', getU32Decoder()],
    ['ticketCount', getU32Decoder()],
    ['endTime', getI64Decoder()],
    ['bump', getU8Decoder()],
    ['winner', getAddressDecoder()],
    ['tickets', getArrayDecoder(getTicketEntryDecoder())],
    ['pendingWinner', getOptionDecoder(getAddressDecoder())],
    ['pendingPrize', getU64Decoder()],
    ['paidOut', getBooleanDecoder()],
    ['status', getVaultStatusDecoder()],
  ]);
}

/** Gets the codec for {@link Vault} account data. */
export function getVaultCodec(): Codec<VaultArgs, Vault> {
  return combineCodec(getVaultEncoder(), getVaultDecoder());
}

export function decodeVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Vault, TAddress>;
export function decodeVault<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Vault, TAddress>;
export function decodeVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Vault, TAddress> | MaybeAccount<Vault, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVaultDecoder()
  );
}

export async function fetchVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Vault, TAddress>> {
  const maybeAccount = await fetchMaybeVault(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Vault, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVault(maybeAccount);
}

export async function fetchAllVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Vault>[]> {
  const maybeAccounts = await fetchAllMaybeVault(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Vault>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVault(maybeAccount));
}
